using UnityEngine;
using System.Collections;

public class HelperPoints : MonoBehaviour {

    [Tooltip("Order where the helper is going to be call")]
    /// <summary>
    /// Order which is going to be call
    /// </summary>
    public float m_fHelperOrder = -1;
}
